require('./animation.js');
require('./weaponsComponents.js');//++
require('./sounds.js');
//require('./parachute.js');
require('./BloodVfx.js');
//require('./sit.js');
//require('./attach.js');


/*


mp.keys.bind(global.Keys.VK_LEFT, true, function () {
    if (!global.loggedin || true) return;
    mp.events.callRemote('server.sc.left');
});
mp.keys.bind(global.Keys.VK_RIGHT, true, function () {
    if (!global.loggedin || true) return;
    mp.events.callRemote('server.sc.right');
});
mp.keys.bind(global.Keys.VK_UP, true, function () {
    if (!global.loggedin || true) return;
    mp.events.callRemote('server.sc.up');
});
mp.keys.bind(global.Keys.VK_DOWN, true, function () {
    if (!global.loggedin || true) return;
    mp.events.callRemote('server.sc.down');
});
mp.keys.bind(global.Keys.VK_SPACE, true, function () {
    if (!global.loggedin || true) return;
    mp.events.callRemote('server.sc.save');
});*/